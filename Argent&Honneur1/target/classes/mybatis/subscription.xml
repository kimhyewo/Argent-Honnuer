<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="subscription">

	<insert id="subscribing">
		INSERT INTO subscription
		(userId, amount, startDate, endDate, cardNumber, expiryDate, cvv)
		VALUES (#{userId}, #{amount}, #{startDate}, #{endDate}, #{cardNumber}, #{expiryDate}, #{cvv})
	</insert>

	<resultMap type="Subscription" id="SubscriptionMap" autoMapping="true"> 
		<id column="userid" property="userid"/>   
	</resultMap>
	
	<select id="item" resultMap="SubscriptionMap">
		SELECT S.*
		FROM subscription S
		
	</select>
	
	<select id="list" resultMap="SubscriptionMap">
		SELECT S.*
		FROM subscription S
		WHERE userid=#{userid}
	</select>
	
</mapper>
